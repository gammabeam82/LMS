{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}

{% block title %}{{ 'genre.edit' | trans }}{% endblock %}

{% block body %}

	<div class="modal fade" id="Modal" tabindex="-1" role="dialog">
		<div class="modal-dialog" role="document">
			<div class="modal-content">
				<div class="modal-header">
					<button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
					<h4 class="modal-title" id="myModalLabel">{{ 'messages.caution' | trans }}</h4>
				</div>
				<div class="modal-body">
					{{ 'messages.genre_delete' | trans }} <span class="genre-name"></span>?
				</div>
				<div class="modal-footer">
					<button type="button" class="btn btn-default" data-dismiss="modal">{{ 'actions.cancel' | trans }}</button>
					<a type="button" class="btn btn-danger delete-genre" href="#">
						<i class="glyphicon glyphicon-remove"></i> {{ 'actions.delete' | trans }}
					</a>
				</div>
			</div>
		</div>
	</div>

	<div class="container-fluid">

		<div class="row">
			<div class="col-xs-12">
				<ol class="breadcrumb">
					<li><a href="{{ path('genres') }}">{{ 'genre.genres' | trans }}</a></li>
					<li class="active">{{ 'genre.edit' | trans }}</li>
				</ol>
			</div>
		</div>

		<div class="row">
			<div class="col-xs-6">

				{{ form_start(form) }}
				{{ form_row(form.name) }}

				<button type="submit" class="btn btn-success pull-right">
					<i class="glyphicon glyphicon-plus"></i> {{ 'actions.save' | trans }}
				</button>

				<button data-name="{{ genre.name }}" data-toggle="modal" data-target="#Modal" class="btn btn-danger delete pull-right" data-path="{{ path('genres_delete', {'id': genre.id}) }}">
					<i class="glyphicon glyphicon-remove"></i> {{ 'actions.delete' | trans }}
				</button>

				{{ form_end(form) }}
			</div>
		</div>
	</div>
{% endblock %}

{% block javascripts %}
	{{ parent() }}
	<script src="{{ asset('assets/js/genres-edit.js') }}"></script>
{% endblock %}